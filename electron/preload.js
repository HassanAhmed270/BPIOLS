// Intentionally empty: the renderer needs no Node/Electron APIs.
// contextIsolation + no exposed bridge keeps the existing React app's
// fetch()-based API calls as the only communication path with the backend.
