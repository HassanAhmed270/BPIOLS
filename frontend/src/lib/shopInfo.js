// Printed at the top of every customer receipt (Billing.jsx). Plain
// constants rather than a settings screen — no Settings model exists yet
// in this codebase — so edit these three lines directly if the shop's
// name/address/phone ever changes.
//
// Defaults below match the sample receipt used to design this layout
// (Receipt_INV-10.pdf) — update if that isn't the correct shop info.
export const SHOP_NAME = 'ITEMS HUB';
export const SHOP_ADDRESS = 'Bolten Market, Karachi';
export const SHOP_PHONE = '0314-2595040';
